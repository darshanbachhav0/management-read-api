{
  "name": "uma-management-read-api",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Read-only external API gateway for UMA Finance Management dashboards and reports.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "generate:key": "node scripts/generateApiKey.js",
    "render:start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0"
  }
}
